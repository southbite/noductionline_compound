<%- javascript_include_tag('jquery-1.7.1-min','jquery.easyui.min','application') %>
<%- stylesheet_link_tag('noductionstyle') %>

<script>

$(document).ready(function() {
	try
	{
		
		//showMessage("Error Message", "error", 5000);
		
		/*
		$("#accountTabs").tabs({
		 onSelect:function(title, index){  
		      resizeComponents();
		      tabSelected(index);
		    }
		});
		*/
		
		//http://www.cssplay.co.uk/menus/vertical_tab_two.html?current=one
	}
	catch(err)
	{
		console.log(err.toString());
	}
 }); 

</script>

<% var form = formFor(project, { action: path_to.account_project(params.account_id, project) }); %>
<%- form.begin() %>

<%- form.label('name', 'name') %> <%- form.input('name') %>
<%- form.submit('Save') %>

<%- form.end() %>